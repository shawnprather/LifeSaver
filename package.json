{
  "name": "lifesaver",
  "version": "1.0.0",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && copyfiles manifest.json dist/ && copyfiles -u 1 src/assests/* src/popup/*.html dist/",
    "watch": "tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^6.0.3"
  }
}
